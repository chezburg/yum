# Optional local-model dependencies.
# Install these only if you use a local OCR engine:
#   OCR_ENGINE=paddleocr  -> paddleocr + paddlepaddle
#
# Note: local Speech-to-Text no longer runs in-process. Run any OpenAI-compatible
# Whisper server (e.g. speaches, faster-whisper-server) and point the STT engine's
# API base URL at it.

paddleocr>=2.8.0
paddlepaddle>=2.6.0
